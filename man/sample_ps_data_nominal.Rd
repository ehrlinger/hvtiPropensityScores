% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-data.R
\name{sample_ps_data_nominal}
\alias{sample_ps_data_nominal}
\title{Generate sample data with a nominal treatment for ps_nominal() examples}
\usage{
sample_ps_data_nominal(n = 200L, seed = 42L)
}
\arguments{
\item{n}{Number of patients per treatment level.  Total rows = \code{4 * n}.
Default \code{200L}.}

\item{seed}{Random seed.  Default \code{42L}.}
}
\value{
A data frame with \code{4 * n} rows and columns: \code{id}, \code{rtyp}
(unordered factor), \code{age}, \code{female}, \code{ef}, \code{diabetes}, \code{hypertension}.
}
\description{
Produces a synthetic data frame with a four-level nominal treatment
variable (\code{rtyp}: tricuspid repair types COS, PER, DEV, CE), mimicking
the dataset used in \code{tp.lm.logistic_propensity.polytomous.sas}.
}
\examples{
dta <- sample_ps_data_nominal(n = 100, seed = 7)
table(dta$rtyp)
str(dta)

}
\seealso{
\code{\link[=ps_nominal]{ps_nominal()}}
}
