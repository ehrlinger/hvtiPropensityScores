% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-sensitivity.R
\name{sa_overlap}
\alias{sa_overlap}
\title{Propensity Score Overlap and Positivity Diagnostics}
\usage{
sa_overlap(
  x,
  score_col = "prob_t",
  treatment_col = "tavr",
  trim_threshold = 0.05
)
}
\arguments{
\item{x}{A \code{ps_data} object \strong{or} a plain data frame.}

\item{score_col}{Name of the propensity score column.  Taken from
\code{x$meta$score_col} when \code{x} is a \code{ps_data} object.  Default \code{"prob_t"}.}

\item{treatment_col}{Name of the binary treatment column (0/1).  Taken from
\code{x$meta$treatment_col} when \code{x} is a \code{ps_data} object\verb{.  Default }"tavr"`.}

\item{trim_threshold}{Numeric in (0, 0.5).  PS values below this threshold
or above \code{1 - trim_threshold} are flagged as near-positivity violations.
Default \code{0.05}.}
}
\value{
A list with:
\describe{
\item{\verb{$summary}}{Data frame: one row per treatment group with \code{group},
\code{n}, \code{ps_min}, \code{ps_q25}, \code{ps_median}, \code{ps_mean}, \code{ps_q75}, \code{ps_max}.}
\item{\verb{$overlap_region}}{Named numeric vector: \code{lower} and \code{upper} bounds
of the empirical common support region.}
\item{\verb{$outside_overlap}}{Data frame: \code{group}, \code{n_outside},
\code{pct_outside} -- patients whose PS is outside the overlap region.}
\item{\verb{$positivity_flags}}{Data frame: \code{group}, \code{n_near_zero}
(PS < \code{trim_threshold}), \code{n_near_one} (PS > \code{1 - trim_threshold}),
\code{pct_near_zero}, \code{pct_near_one}.}
}
}
\description{
Examines the propensity score (PS) distribution for potential positivity
violations -- regions of covariate space where the probability of receiving
one treatment is near zero -- and quantifies the common support region.
}
\details{
The function reports:
\itemize{
\item PS summary statistics by treatment group.
\item The empirical overlap region: [max(min_treated, min_control),
min(max_treated, max_control)].
\item The proportion of each group whose PS lies outside the overlap region.
\item The number and proportion of patients with PS below \code{trim_threshold} or
above \code{1 - trim_threshold} (near-positivity-violation flags).
}
}
\examples{
dta <- sample_ps_data(n = 400, seed = 7)
res <- sa_overlap(dta, score_col = "prob_t", treatment_col = "tavr")
res$overlap_region
res$positivity_flags

# From a ps_match object
m   <- ps_match(dta)
res <- sa_overlap(m)
res$outside_overlap

}
\seealso{
\code{\link[=ps_match]{ps_match()}}, \code{\link[=ps_weight]{ps_weight()}}, \code{\link[=sa_rosenbaum]{sa_rosenbaum()}}, \code{\link[=sa_evalue]{sa_evalue()}},
\code{\link[=sa_trim_sweep]{sa_trim_sweep()}}
}
