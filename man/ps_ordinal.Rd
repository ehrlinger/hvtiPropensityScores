% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-score.R
\name{ps_ordinal}
\alias{ps_ordinal}
\title{Estimate propensity scores via ordinal (cumulative logit) logistic
regression}
\usage{
ps_ordinal(
  formula,
  data,
  treatment_col = NULL,
  id_col = "id",
  imputation_col = NULL,
  score_col_prefix = "prob",
  covariates = NULL
)
}
\arguments{
\item{formula}{A formula with the ordered factor treatment on the
LHS, e.g. \code{nyha_grp ~ age + female + ef}.  The response is coerced to
an ordered factor if it is not already one.}

\item{data}{A data frame (single or stacked MI).}

\item{treatment_col}{Name of the ordinal treatment column.  \code{NULL}
(default) extracts from \code{formula}.}

\item{id_col}{Patient identifier column.  Default \code{"id"}.}

\item{imputation_col}{Imputation-index column for stacked MI data.
\code{NULL} (default) assumes a single complete dataset.}

\item{score_col_prefix}{Prefix for the per-level output columns.  Columns
are named \verb{<prefix>_<level>} for each level of the treatment.
Default \code{"prob"}.}

\item{covariates}{Covariate columns for diagnostics.}
}
\value{
An object of class \code{c("ps_ordinal", "ps_data")} with:
\describe{
\item{\verb{$data}}{Base data frame with one \verb{prob_<level>} column per
treatment level.}
\item{\verb{$meta}}{Named list: \code{formula}, \code{treatment_col}, \code{id_col},
\code{imputation_col}, \code{score_cols}, \code{levels}, \code{method},
\code{n_imputations}, \code{n_total}.}
\item{\verb{$tables}}{Named list: \code{group_counts}.}
}
}
\description{
Fits a proportional-odds cumulative logit model via \code{\link[MASS:polr]{MASS::polr()}} for an
ordered treatment variable and appends one probability column per
treatment level to the dataset.  The model is equivalent to the default
\verb{PROC LOGISTIC} behaviour for ordinal responses in SAS.
}
\details{
When \code{imputation_col} is supplied, models are fit on each imputed dataset
separately and per-level probabilities are averaged per patient across
imputations.
}
\section{Package dependency}{

Requires the \pkg{MASS} package (a recommended package shipped with
base R).  Install with \code{install.packages("MASS")} if missing.
}

\examples{
\dontrun{
dta <- sample_ps_data_ordinal(n = 300, seed = 42)
obj <- ps_ordinal(
  nyha_grp ~ age + female + ef + diabetes,
  data = dta
)
print(obj)
head(obj$data[, c("id", "nyha_grp", "prob_I", "prob_II", "prob_III")])
}

}
\seealso{
\code{\link[=ps_logistic]{ps_logistic()}}, \code{\link[=ps_nominal]{ps_nominal()}}, \code{\link[=ps_match]{ps_match()}}
}
