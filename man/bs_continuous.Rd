% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bs-score.R
\name{bs_continuous}
\alias{bs_continuous}
\title{Estimate a balancing score via linear regression}
\usage{
bs_continuous(
  formula,
  data,
  outcome_col = NULL,
  id_col = "id",
  imputation_col = NULL,
  score_col = "bs",
  n_strata = 10L,
  strata_col = "cluster",
  create_binary_strata = TRUE,
  covariates = NULL
)
}
\arguments{
\item{formula}{A formula with the continuous outcome / exposure
on the LHS, e.g.
\code{hct_nadr ~ female + agee + in_bmi + hct_pr + iv_cpb}.}

\item{data}{A data frame, or a stacked MI data frame when
\code{imputation_col} is set.}

\item{outcome_col}{Name of the continuous outcome column.  \code{NULL}
(default) extracts from \code{formula}.}

\item{id_col}{Patient identifier column.  Required when
\code{imputation_col} is set.  Default \code{"id"}.}

\item{imputation_col}{Imputation-index column for stacked MI data.
\code{NULL} (default) assumes a single complete dataset.}

\item{score_col}{Name of the output balancing score column
(averaged fitted value).  Default \code{"bs"}.}

\item{n_strata}{Number of strata (clusters).  Also controls how
many binary indicator columns are created when
\code{create_binary_strata = TRUE}.  Default \code{10L}.}

\item{strata_col}{Name of the cluster/stratum column appended to
\verb{$data}.  Default \code{"cluster"}.}

\item{create_binary_strata}{Logical.  If \code{TRUE} (default), binary columns
\code{stra_1} through \verb{stra_<n_strata>} are appended, mirroring the \verb{\%grp}
macro in the SAS template.}

\item{covariates}{Covariate columns for diagnostics (not used in
computation; reserved for future balance-checking helpers).}
}
\value{
An object of class \code{c("bs_continuous", "ps_data")} with:
\describe{
\item{\verb{$data}}{Base data frame with \code{score_col}, \code{"quintile"},
\code{"decile"}, \code{strata_col}, and (if \code{create_binary_strata}) \code{stra_1}
through \verb{stra_<n_strata>} appended.}
\item{\verb{$meta}}{Named list: \code{formula}, \code{outcome_col}, \code{id_col},
\code{imputation_col}, \code{score_col}, \code{n_strata}, \code{strata_col}, \code{method},
\code{n_imputations}, \code{n_total}.}
\item{\verb{$tables}}{Named list: \code{strata_counts}.}
}
}
\description{
Fits \code{stats::lm()} on a (possibly multiply-imputed) dataset, averages the
per-patient \strong{fitted values} across imputations, and assigns rank-based
quintile, decile, and cluster strata.  Mirrors the workflow in
\code{tp.rm.continuous.balncing_score.sas} (PROC REG + PROC SUMMARY +
the \verb{\%grp} macro for binary stratum indicators).
}
\details{
The balancing score is the expected value of the continuous exposure
(e.g. nadir haematocrit) given the full covariate set.  Patients in the
same stratum have similar expected exposure values, making within-stratum
comparisons less confounded.
}
\examples{
# Use ef (ejection fraction) as a stand-in for a continuous exposure
dta <- sample_ps_data(n = 300, seed = 42)
obj <- bs_continuous(
  ef ~ age + female + diabetes + hypertension,
  data    = dta,
  id_col  = "id"
)
print(obj)
table(obj$data$cluster)

}
\seealso{
\code{\link[=bs_count]{bs_count()}}, \code{\link[=ps_logistic]{ps_logistic()}}, \code{\link[=sample_ps_data]{sample_ps_data()}}
}
