% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-weight.R
\name{ps_weight}
\alias{ps_weight}
\title{Inverse-Probability-of-Treatment Weighting (IPTW)}
\usage{
ps_weight(
  data,
  treatment_col = "tavr",
  score_col = "prob_t",
  estimand = c("ATE", "ATT", "ATC"),
  stabilise = TRUE,
  trim = NULL,
  covariates = NULL,
  weight_col = "iptw"
)
}
\arguments{
\item{data}{A data frame.  Must contain \code{treatment_col} and
\code{score_col}.}

\item{treatment_col}{Name of the binary treatment column (0/1 or logical).
Default \code{"tavr"}.}

\item{score_col}{Name of the numeric propensity score column (values
in [0, 1]).  Default \code{"prob_t"}.}

\item{estimand}{Causal estimand.  One of:
\itemize{
\item \code{"ATE"} (default) — average treatment effect.
Treated weights = \code{1/ps}; control weights = \code{1/(1-ps)}.
\item \code{"ATT"} — average treatment effect on the treated.
Treated weights = \code{1}; control weights = \code{ps/(1-ps)}.
\item \code{"ATC"} — average treatment effect on the controls.
Treated weights = \code{(1-ps)/ps}; control weights = \code{1}.
}}

\item{stabilise}{Logical.  If \code{TRUE} (default), weights are
stabilised by multiplying by the marginal treatment probability.}

\item{trim}{Optional numeric in (0, 0.5).  If supplied, weights
outside the (\code{trim}, \code{1 - trim}) quantile range are winsorised.
Default \code{NULL} (no trimming).}

\item{covariates}{Character vector of covariate columns for SMD
diagnostics.  If \code{NULL} (default), all numeric columns other than
\code{treatment_col} and \code{score_col} are used.}

\item{weight_col}{Name of the output weight column appended to \verb{$data}.
Default \code{"iptw"}.  If the column already exists it is overwritten.}
}
\value{
An object of class \code{c("ps_weight", "ps_data")} with:
\describe{
\item{\verb{$data}}{The input data frame with \code{weight_col} appended.}
\item{\verb{$meta}}{Named list: \code{treatment_col}, \code{score_col}, \code{weight_col},
\code{estimand}, \code{stabilised}, \code{trim}, \code{method}, \code{n_total}.}
\item{\verb{$tables}}{Named list: \code{smd_unweighted}, \code{smd_weighted},
\code{group_counts}, \code{effective_n}.}
}
}
\description{
Computes IPTW weights from a pre-computed propensity score column and
appends them to the dataset.  Supports ATE, ATT, and ATC estimands.
Returns a \code{ps_weight} object whose \verb{$tables} slot contains SMD tables
(unweighted and weighted) and effective-N diagnostics compatible with
\code{\link[hvtiPlotR:hv_mirror_hist]{hvtiPlotR::hv_mirror_hist()}} (weighted mode) and
\code{\link[hvtiPlotR:hv_balance]{hvtiPlotR::hv_balance()}}.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 42)

# 1. ATE weights (default)
obj <- ps_weight(dta)
print(obj)
summary(obj)

# 2. ATT weights, stabilised
obj_att <- ps_weight(dta, estimand = "ATT", stabilise = TRUE)
summary(obj_att)

# 3. Extract data with weights appended
head(obj$data[, c("id", "tavr", "prob_t", "iptw")])

}
\seealso{
\code{\link[=ps_match]{ps_match()}}, \code{\link[=sample_ps_data]{sample_ps_data()}},
\code{hvtiPlotR::hv_mirror_hist()}, \code{hvtiPlotR::hv_balance()}
}
