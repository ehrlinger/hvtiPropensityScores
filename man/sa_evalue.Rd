% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-sensitivity.R
\name{sa_evalue}
\alias{sa_evalue}
\title{E-Value Sensitivity Analysis}
\usage{
sa_evalue(
  estimate,
  ci_lo = NULL,
  ci_hi = NULL,
  type = c("RR", "OR", "HR", "RD"),
  p0 = NULL
)
}
\arguments{
\item{estimate}{Numeric.  The point estimate (RR, OR, HR, or RD on the
original scale).  Must be > 0 for RR/OR/HR; any finite value for RD.}

\item{ci_lo}{Optional numeric.  Lower confidence interval bound.}

\item{ci_hi}{Optional numeric.  Upper confidence interval bound.
Both \code{ci_lo} and \code{ci_hi} must be supplied together to compute the CI
E-value.}

\item{type}{One of \code{"RR"} (default), \code{"OR"}, \code{"HR"}, or \code{"RD"}.}

\item{p0}{Baseline outcome probability in the unexposed group.
Required when \code{type = "RD"}.  Must be in (0, 1).}
}
\value{
A list with:
\describe{
\item{\verb{$evalue_estimate}}{E-value for the point estimate.}
\item{\verb{$evalue_ci}}{E-value for the confidence interval bound closest to
the null.  \code{NA} if \code{ci_lo}/\code{ci_hi} are not supplied.  Returns \code{1} if
the CI already crosses the null.}
\item{\verb{$rr_estimate}}{The point estimate converted to the risk-ratio
scale (same as \code{estimate} when \code{type = "RR"}).}
\item{\verb{$type}}{The measure type supplied.}
}
}
\description{
Computes the E-value for a point estimate and optionally for a confidence
interval bound, following VanderWeele & Ding (2017).  The E-value is the
minimum strength of association (on the risk-ratio scale) that an unmeasured
confounder would need with \strong{both} the treatment and the outcome,
conditional on measured covariates, to fully explain away the observed
association.
}
\details{
For odds ratios and hazard ratios the rare-outcome approximation
\code{RR_approx = sqrt(OR)} / \code{sqrt(HR)} is used to convert to the risk-ratio
scale before applying the E-value formula.  For risk differences a baseline
outcome probability \code{p0} must be supplied.
}
\examples{
# Risk ratio of 2.5 -- how robust is the result?
sa_evalue(2.5, ci_lo = 1.8, ci_hi = 3.5)

# Odds ratio of 0.6 (protective effect)
sa_evalue(0.6, ci_lo = 0.4, ci_hi = 0.9, type = "OR")

# Risk difference of 0.10 with baseline probability 0.20
sa_evalue(0.10, ci_lo = 0.02, ci_hi = 0.18, type = "RD", p0 = 0.20)

}
\references{
VanderWeele, T. J., & Ding, P. (2017).  Sensitivity analysis in
observational research: Introducing the E-value.  \emph{Annals of Internal
Medicine}, 167(4), 268-274.  \doi{10.7326/M16-2607}
}
\seealso{
\code{\link[=sa_rosenbaum]{sa_rosenbaum()}}, \code{\link[=sa_overlap]{sa_overlap()}}, \code{\link[=sa_trim_sweep]{sa_trim_sweep()}}
}
