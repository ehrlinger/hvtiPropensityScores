% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ps-match.R
\name{ps_match}
\alias{ps_match}
\title{Propensity Score Nearest-Neighbour Matching}
\usage{
ps_match(
  data,
  treatment_col = "tavr",
  score_col = "prob_t",
  id_col = "id",
  caliper = NULL,
  covariates = NULL,
  match_col = "match",
  ratio = 1L,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.  Must contain \code{treatment_col},
\code{score_col}, and \code{id_col}.}

\item{treatment_col}{Name of the binary treatment column (0/1 or
logical).  Default \code{"tavr"}.}

\item{score_col}{Name of the numeric propensity score column
(values in [0, 1]).  Default \code{"prob_t"}.}

\item{id_col}{Name of the patient identifier column.
Default \code{"id"}.}

\item{caliper}{Maximum allowable difference in propensity score
for a match to be accepted.  \code{NULL} (default) imposes no caliper.
Typical values: \code{0.1 * sd(score)} (20\% of SD rule) or \code{0.2}.}

\item{covariates}{Character vector of covariate columns for SMD
diagnostics.  If \code{NULL} (default), all numeric columns other than
\code{treatment_col} and \code{score_col} are used.}

\item{match_col}{Name of the output match-indicator column appended
to \verb{$data}.  Default \code{"match"}.  If the column already exists it is
overwritten.}

\item{ratio}{Matching ratio (controls : treated).  Currently
only \code{1} (1:1) is supported.  Default \code{1}.}

\item{seed}{Integer seed for the random shuffling of treated-
patient order that reduces order-dependence.  Pass an integer for a
reproducible matched set; \code{NULL} (default) uses the session's current
RNG state (not reproducible across sessions).}
}
\value{
An object of class \code{c("ps_match", "ps_data")} with:
\describe{
\item{\verb{$data}}{The full input data frame with \code{match_col} and \code{pair_id}
appended.  \code{match_col} is \code{1} for matched patients and \code{0} otherwise.
\code{pair_id} is an integer identifying which matched pair each patient
belongs to (\code{NA} for unmatched patients).  Filter to \code{match == 1} to
obtain the matched subset.}
\item{\verb{$meta}}{Named list: \code{treatment_col}, \code{score_col}, \code{id_col},
\code{match_col}, \code{pair_id_col}, \code{caliper}, \code{method}, \code{n_total},
\code{n_matched}, \code{n_unmatched}.}
\item{\verb{$tables}}{Named list: \code{smd_before}, \code{smd_after},
\code{group_counts_before}, \code{group_counts_after}.}
}
}
\description{
Performs 1:1 nearest-neighbour matching without replacement on a
pre-computed propensity score column.  Returns a \code{ps_match} object whose
\verb{$data} slot is the \strong{full} original dataset with a \code{match} column set to
\code{1} for matched pairs and \code{0} for unmatched patients.
}
\details{
The \verb{$tables} slot contains standardised mean difference (SMD) tables
before and after matching, and group counts — the same diagnostics
expected by \code{\link[hvtiPlotR:hv_mirror_hist]{hvtiPlotR::hv_mirror_hist()}} and
\code{\link[hvtiPlotR:hv_balance]{hvtiPlotR::hv_balance()}}.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 42)

# 1. Build the matched object
obj <- ps_match(dta)

# 2. Inspect diagnostics
print(obj)
summary(obj)

# 3. Extract the full data (with match indicator)
head(obj$data)
table(obj$data$match)

# 4. Matched subset only
matched <- obj$data[obj$data$match == 1L, ]
nrow(matched)

}
\seealso{
\code{\link[=ps_weight]{ps_weight()}}, \code{\link[=sample_ps_data]{sample_ps_data()}},
\code{hvtiPlotR::hv_mirror_hist()}, \code{hvtiPlotR::hv_balance()}
}
