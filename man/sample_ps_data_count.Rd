% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-data.R
\name{sample_ps_data_count}
\alias{sample_ps_data_count}
\title{Generate sample data with a count outcome for bs_count() examples}
\usage{
sample_ps_data_count(n = 500L, seed = 42L, n_imputations = 1L)
}
\arguments{
\item{n}{Number of patients.  Default \code{500L}.}

\item{seed}{Random seed.  Default \code{42L}.}

\item{n_imputations}{If \verb{> 1}, returns a stacked multiply-imputed data
frame with an \verb{_IMPUTATION_} index column containing \code{n_imputations}
replicates (each with slightly jittered covariate values to mimic
imputed datasets).  Default \code{1L} (single complete dataset).}
}
\value{
A data frame with columns: \code{id}, \code{rbc_tot} (count), \code{age},
\code{female}, \code{diabetes}, \code{hypertension}, \code{hct_pr} (pre-op haematocrit),
\code{cpb_time} (cardiopulmonary bypass time, minutes), and optionally
\verb{_IMPUTATION_} when \code{n_imputations > 1}.
}
\description{
Produces a synthetic data frame with a Poisson-distributed count outcome
(\code{rbc_tot}, total intra-operative RBC units transfused), along with
baseline covariates and an optional multiply-imputed stacked format.
Mirrors the dataset used in \code{tp.pm.count.balncing_score.sas}.
}
\examples{
dta <- sample_ps_data_count(n = 200, seed = 7)
hist(dta$rbc_tot, main = "RBC units distribution")

# Stacked multiply-imputed version (3 imputations)
dta_mi <- sample_ps_data_count(n = 200, seed = 7, n_imputations = 3)
table(dta_mi[["_IMPUTATION_"]])

}
\seealso{
\code{\link[=bs_count]{bs_count()}}, \code{\link[=bs_continuous]{bs_continuous()}}
}
