% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-sensitivity.R
\name{sa_rosenbaum}
\alias{sa_rosenbaum}
\title{Rosenbaum Sensitivity Bounds for Matched Analyses}
\usage{
sa_rosenbaum(
  x,
  outcome_col,
  gamma_max = 3,
  gamma_inc = 0.25,
  alpha = 0.05,
  treatment_col = NULL,
  pair_id_col = "pair_id"
)
}
\arguments{
\item{x}{A \code{ps_match} object (returned by \code{\link[=ps_match]{ps_match()}}) \strong{or} a
plain data frame whose rows are matched patients.}

\item{outcome_col}{Name of the numeric outcome column.  Required.}

\item{gamma_max}{Maximum Gamma to test.  Default \code{3}.}

\item{gamma_inc}{Increment between Gamma values.  Default \code{0.25}.}

\item{alpha}{Significance threshold.  Default \code{0.05}.}

\item{treatment_col}{Name of the binary treatment column (0/1).  Taken from
\code{x$meta$treatment_col} when \code{x} is a \code{ps_match} object.  Required when
\code{x} is a plain data frame.}

\item{pair_id_col}{Name of the pair-ID column created by \code{\link[=ps_match]{ps_match()}}.
Default \code{"pair_id"}.}
}
\value{
A list with three elements:
\describe{
\item{\verb{$bounds}}{Data frame with columns \code{gamma}, \code{p_upper}, \code{p_lower},
\code{reject_upper} (logical -- is the upper bound < \code{alpha}?).}
\item{\verb{$sensitivity_value}}{Largest Gamma for which the analysis remains
significant (upper p-value < \code{alpha}), or \code{NA} if the initial analysis
is not significant.}
\item{\verb{$n_pairs}}{Number of complete matched pairs used.}
}
}
\description{
Assesses how sensitive a matched-pair analysis is to unmeasured confounding
using the Rosenbaum bounds framework.  For each value of the sensitivity
parameter Gamma -- the maximum odds ratio for differential treatment
assignment within a matched pair due to hidden bias -- the function reports
the upper and lower bounds on the p-value of the Wilcoxon signed-rank test.
}
\details{
A Gamma of 1 corresponds to no hidden bias (equivalent to perfect
randomisation within pairs).  The \emph{sensitivity value} is the largest Gamma
at which the upper p-value bound remains below \code{alpha}; a larger sensitivity
value indicates a more robust result.
}
\examples{
dta <- sample_ps_data(n = 300, seed = 1)
m   <- ps_match(dta)
res <- sa_rosenbaum(m, outcome_col = "ef", gamma_max = 2)
res$bounds
res$sensitivity_value

}
\references{
Rosenbaum, P. R. (2002). \emph{Observational Studies} (2nd ed.). Springer.
}
\seealso{
\code{\link[=ps_match]{ps_match()}}, \code{\link[=sa_evalue]{sa_evalue()}}, \code{\link[=sa_overlap]{sa_overlap()}}, \code{\link[=sa_trim_sweep]{sa_trim_sweep()}}
}
