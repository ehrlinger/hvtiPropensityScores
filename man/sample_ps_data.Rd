% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-data.R
\name{sample_ps_data}
\alias{sample_ps_data}
\title{Generate sample data for propensity score examples}
\usage{
sample_ps_data(n = 500L, seed = 42L, separation = 1)
}
\arguments{
\item{n}{Number of patients per treatment arm.  Total rows =
\code{2 * n}.  Must be a positive integer.  Default \code{500}.}

\item{seed}{Random seed for reproducibility.  Default \code{42L}.}

\item{separation}{Numeric scalar controlling how well the covariates
separate the two groups.  Higher values → less overlap.  Default \code{1.0}.}
}
\value{
A data frame with \code{2 * n} rows and the following columns:
\describe{
\item{\code{id}}{Integer patient identifier.}
\item{\code{tavr}}{Binary treatment indicator: \code{0} = SAVR, \code{1} = TF-TAVR.}
\item{\code{age}}{Continuous: age in years.}
\item{\code{female}}{Binary: \code{1} = female.}
\item{\code{ef}}{Continuous: ejection fraction (\%).}
\item{\code{diabetes}}{Binary: \code{1} = diabetic.}
\item{\code{hypertension}}{Binary: \code{1} = hypertensive.}
\item{\code{prob_t}}{Estimated propensity score (numeric, 0–1).}
\item{\code{match}}{Match indicator, initialised to \code{0}.  Populated by
\code{\link[=ps_match]{ps_match()}}.}
}
}
\description{
Produces a synthetic data frame that resembles a typical SAS export from a
cardiac-surgery propensity score analysis.  Propensity scores are generated
from a logistic model driven by the supplied covariates, so the resulting
\code{prob_t} column has realistic overlap and the SMD values are non-trivial.
}
\examples{
dta <- sample_ps_data(n = 200, seed = 42)
head(dta)
table(dta$tavr)
hist(dta$prob_t, breaks = 20, main = "Propensity score distribution")

}
\seealso{
\code{\link[=ps_match]{ps_match()}}, \code{\link[=ps_weight]{ps_weight()}}
}
