% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sa-sensitivity.R
\name{sa_trim_sweep}
\alias{sa_trim_sweep}
\title{IPTW Weight-Trimming Sensitivity Sweep}
\usage{
sa_trim_sweep(
  x,
  trim_seq = seq(0, 0.1, by = 0.01),
  estimand = c("ATE", "ATT", "ATC"),
  stabilise = TRUE,
  score_col = "prob_t",
  treatment_col = "tavr"
)
}
\arguments{
\item{x}{A \code{ps_weight} object (returned by \code{\link[=ps_weight]{ps_weight()}}) \strong{or}
a \code{ps_match} object \strong{or} a plain data frame that contains a propensity
score column and a binary treatment column.}

\item{trim_seq}{Numeric vector of trimming values to evaluate.  Each
value is the lower tail quantile; weights outside the
(\code{trim}, \code{1 - trim}) range are winsorised.  \code{0} means no trimming.
Default \code{seq(0, 0.10, by = 0.01)}.}

\item{estimand}{One of \code{"ATE"}, \code{"ATT"}, or \code{"ATC"} -- the causal
estimand used to compute IPTW weights.  Taken from \code{x$meta$estimand}
when \code{x} is a \code{ps_weight} object; defaults to \code{"ATE"} otherwise.}

\item{stabilise}{Logical.  Whether to stabilise weights.  Taken from
\code{x$meta$stabilised} when available; defaults to \code{TRUE}.}

\item{score_col}{Name of the propensity score column.  Taken from
\code{x$meta$score_col} when \code{x} is a \code{ps_data} object.  Default \code{"prob_t"}.}

\item{treatment_col}{Name of the binary treatment column.  Taken from
\code{x$meta$treatment_col} when \code{x} is a \code{ps_data} object\verb{.  Default }"tavr"`.}
}
\value{
A data frame with one row per trim value and columns:
\describe{
\item{\code{trim}}{The winsorisation threshold.}
\item{\code{ess_control}}{Effective sample size for the control group.}
\item{\code{ess_treated}}{Effective sample size for the treated group.}
\item{\code{max_weight}}{Maximum weight across all patients after trimming.}
\item{\code{sd_weight}}{Standard deviation of weights after trimming.}
\item{\code{pct_trimmed}}{Percentage of patients whose weight was winsorised.}
}
}
\description{
Re-computes IPTW weights across a range of winsorisation thresholds and
reports how the weight distribution and effective sample size (ESS) change
as trimming increases.  This helps identify a trimming threshold that
adequately controls extreme weights without an unacceptable loss of effective
sample size.
}
\examples{
dta    <- sample_ps_data(n = 400, seed = 3)
w_obj  <- ps_weight(dta, estimand = "ATE")
sweep  <- sa_trim_sweep(w_obj)
head(sweep)

# Directly from a data frame
sweep2 <- sa_trim_sweep(dta, score_col = "prob_t", treatment_col = "tavr",
                        estimand = "ATE")

}
\seealso{
\code{\link[=ps_weight]{ps_weight()}}, \code{\link[=sa_rosenbaum]{sa_rosenbaum()}}, \code{\link[=sa_evalue]{sa_evalue()}}, \code{\link[=sa_overlap]{sa_overlap()}}
}
